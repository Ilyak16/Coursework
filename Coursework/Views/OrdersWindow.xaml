<Window x:Class="Coursework.Views.OrdersWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Заказы"
        Height="600"
        Width="1000">

    <Grid Margin="10">

        <Grid.RowDefinitions>

            <RowDefinition/>
            <RowDefinition Height="Auto"/>

        </Grid.RowDefinitions>

        <DataGrid x:Name="OrdersGrid"
                  AutoGenerateColumns="True"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Просмотр деталей" Width="150" Margin="5" Click="ViewDetails_Click"/>
            <Button Content="Одобрить" Width="120" Margin="5" Click="Approve_Click"/>
            <Button Content="Отклонить" Width="120" Margin="5" Background="Red" Foreground="White" Click="Decline_Click"/>
        </StackPanel>

    </Grid>
</Window>